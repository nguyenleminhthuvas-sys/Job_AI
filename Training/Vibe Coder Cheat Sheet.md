## 1. WEB ANIMATION EFFECTS

### Awwwards / Premium Effects
| Term                   | Definition                                 | Example Prompt                                                           |
| ---------------------- | ------------------------------------------ | ------------------------------------------------------------------------ |
| `scrollytelling`       | Story unfolds as user scrolls              | "Create scrollytelling page where content reveals in narrative sequence" |
| `horizontal scroll`    | Vertical scroll moves content horizontally | "Make horizontal scroll gallery that moves left as I scroll down"        |
| `immersive experience` | Full-page environmental animation          | "Build immersive experience where entire page transforms on scroll"      |
| `clip path reveal`     | Content revealed through geometric shapes  | "Use clip path reveal to unmask hero image from center"                  |
| `scroll hijacking`     | Custom scroll behavior for sections        | "Implement scroll hijacking for showcase section"                        |

### Three.js / WebGL
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `particle system` | Thousands of animated points | "Create particle system that forms logo then disperses" |
| `particle morphing` | Particles transition between shapes | "Add particle morphing from sphere to product shape" |
| `geometry morphing` | 3D shapes transforming smoothly | "Implement geometry morphing from plane to terrain" |
| `shader effect` | GPU visual manipulation | "Add shader effect for iridescent color shift" |
| `displacement map` | Texture deforms geometry | "Apply displacement effect for liquid image transition" |
| `bloom/glow` | Bright areas emit light | "Add bloom effect to make neon elements glow" |
| `chromatic aberration` | RGB color separation | "Apply chromatic aberration on page transitions" |
| `post-processing` | Effects after render (blur, grain) | "Add post-processing with vignette and film grain" |
| `orbit controls` | User can rotate 3D scene | "Enable orbit controls to explore 3D model" |

### GSAP
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `ScrollTrigger` | Scroll-controlled animations | "Use ScrollTrigger to animate sections entering viewport" |
| `pin/pinning` | Element stays fixed while scrolling | "Pin hero section while feature cards scroll over" |
| `scrub` | Animation tied to scroll position | "Add scrub so rotation matches scroll position exactly" |
| `snap` | Auto-snap to positions | "Enable snap scrolling between full-page sections" |
| `timeline` | Sequenced animations | "Create timeline: logo animates, then tagline, then CTA" |
| `stagger` | Delayed sequential animations | "Stagger card entrance with delay between each" |
| `SplitText` | Animate individual letters/words | "Use SplitText to animate headline letter by letter" |
| `ScrambleText` | Random character decode | "Apply ScrambleText effect on hover" |
| `DrawSVG` | SVG stroke animation | "Add DrawSVG to animate icon being drawn" |
| `MorphSVG` | SVG path transformation | "Use MorphSVG to transform hamburger to X" |
| `Flip` | Animate layout changes | "Use Flip to animate cards when filtering" |

### Framer Motion / React Spring
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `layout animation` | Smooth position/size changes | "Add layout animation so cards reorder smoothly when filtered" |
| `shared element transition` | Element morphs between states | "Create shared element transition from grid to modal" |
| `AnimatePresence` | Animate mount/unmount | "Use AnimatePresence for list items entering/exiting" |
| `variants` | Named animation states | "Define variants for idle, hover, and active states" |
| `gesture animation` | Swipe, drag, tap responses | "Add gesture animation for swipe-to-dismiss" |
| `spring physics` | Natural bounce/momentum | "Use spring physics with bounce for notification entry" |
| `drag constraints` | Limit drag boundaries | "Enable drag with constraints within container" |
| `exit animation` | Animation when unmounting | "Add exit animation fading out and scaling down" |

### Scroll Effects
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `smooth scroll` | Inertia-based scrolling | "Implement smooth scroll with momentum" |
| `Locomotive Scroll` | Premium scroll + parallax | "Add Locomotive Scroll for premium scroll experience" |
| `Lenis` | Lightweight smooth scroll | "Use Lenis for lightweight smooth scrolling" |
| `parallax depth` | Multiple layers at different speeds | "Create 5-layer parallax depth effect on hero" |
| `scroll velocity` | Animation intensity by scroll speed | "Make blur increase with scroll velocity" |
| `reveal on scroll` | Content animates into view | "Create reveal on scroll for testimonial cards" |

### Text Effects
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `typewriter` | Characters appear one by one | "Add typewriter effect to hero headline" |
| `text scramble` | Random characters resolve to text | "Add text scramble effect like hacker terminal" |
| `glitch text` | Digital distortion | "Apply glitch text effect on cyberpunk heading" |
| `text wave` | Characters animate in wave | "Create text wave animation on hover" |
| `kinetic typography` | Dynamic expressive text | "Create kinetic typography for intro sequence" |
| `marquee` | Continuously scrolling text | "Add marquee scrolling announcement bar" |
| `gradient text animation` | Moving gradient across text | "Add animated gradient flowing through headline" |

### Cursor Effects
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `custom cursor` | Replace default cursor | "Create custom cursor as dot with ring" |
| `magnetic cursor` | Elements pull toward cursor | "Add magnetic cursor effect to buttons" |
| `cursor follower` | Element follows mouse with lag | "Create cursor follower that trails behind" |
| `cursor trail` | Visual trail behind cursor | "Add cursor trail with fading particles" |
| `spotlight cursor` | Reveals content under cursor | "Create spotlight cursor revealing hidden content" |
| `cursor text` | Text attached to cursor | "Show 'View' text on cursor over projects" |

### Hover Effects
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `3D tilt` | Card tilts toward cursor | "Add 3D tilt effect to project cards" |
| `card flip` | Card rotates to reveal back | "Create card flip showing details on back" |
| `magnetic hover` | Element subtly follows cursor | "Add magnetic hover to navigation links" |
| `border animation` | Animated border on hover | "Create border animation drawing around on hover" |
| `background slide` | Color slides in on hover | "Add background slide from left on button hover" |
| `underline animation` | Animated link underline | "Add underline animation growing from center" |

### Page Transitions
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `crossfade` | Pages fade between each other | "Add crossfade transition between pages" |
| `wipe transition` | New page revealed by wipe | "Create diagonal wipe transition" |
| `mask transition` | Shape expands to reveal | "Use circular mask transition from click point" |
| `morph transition` | Elements morph between pages | "Add morph transition for shared elements" |
| `stagger exit/enter` | Elements leave/enter in sequence | "Add stagger exit for cards, stagger enter on new page" |
| `View Transition API` | Native browser transitions | "Implement View Transition API for smooth page changes" |

### Image Effects
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `Ken Burns` | Slow pan/zoom on images | "Apply Ken Burns effect to slideshow" |
| `image distortion` | Warping on interaction | "Create image distortion effect on hover" |
| `RGB shift` | Color channel separation | "Apply RGB shift on image hover" |
| `displacement effect` | Liquid-like transitions | "Add displacement effect for liquid image transition" |
| `ripple effect` | Waves from click point | "Create ripple effect on image click" |

### Micro-interactions
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `skeleton loading` | Placeholder shapes | "Show skeleton screen while content loads" |
| `shimmer` | Moving highlight on skeleton | "Add shimmer effect sweeping across skeleton" |
| `ripple feedback` | Material click wave | "Add ripple feedback on button click" |
| `success animation` | Celebration on complete | "Create checkmark success animation" |
| `hamburger animation` | Menu icon morphs | "Create hamburger to X animation" |
| `counter animation` | Numbers animate up/down | "Create counting animation for stats" |

### Lottie / Rive
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `Lottie animation` | Vector animation from After Effects | "Add Lottie animation for success state" |
| `state machine` | Interactive multi-state animation | "Create Rive state machine with idle, hover, active" |
| `interactive animation` | Responds to user input | "Build interactive animation that changes on click" |
| `loop animation` | Continuously repeating | "Create loop animation for loading spinner" |

### Advanced Effects
| Term | Definition | Example Prompt |
|------|------------|----------------|
| `liquid/fluid effect` | Organic flowing motion | "Create liquid effect for hero section" |
| `metaball effect` | Blobs merging and separating | "Add metaball effect where elements connect" |
| `ASCII effect` | Convert visuals to ASCII | "Apply ASCII effect to video on scroll" |
| `noise field` | Animated procedural noise | "Create animated noise field background" |
| `audio reactive` | Animation responds to sound | "Make particles audio reactive to music" |

---

## 2. UI/UX DESIGN PATTERNS

### Visual Styles
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `glassmorphism` | Frosted glass effect | "Glassmorphism card: blur 20px, white 10% opacity, 1px light border" |
| `neumorphism` | Soft extruded shapes | "Neumorphic button with inset shadow on press" |
| `claymorphism` | 3D clay-like elements | "Claymorphism card: rounded 40px, dual shadows, pastel colors" |
| `brutalism` | Raw, bold, unconventional | "Brutalist design: thick black borders, system fonts, high contrast" |
| `minimalism` | Essential elements only | "Minimal design: lots of whitespace, single accent color" |
| `skeuomorphism` | Real-world mimicry | "Skeuomorphic toggle switch with metal texture and depth" |

### Layout Patterns
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `bento grid` | Varied-size grid cells | "Bento grid layout: hero spans 2 cols, smaller cards fill gaps" |
| `masonry` | Pinterest-style stacking | "Masonry layout for image gallery, 4 columns" |
| `holy grail` | Header/footer + 3 columns | "Holy grail layout with sticky sidebar" |
| `sidebar` | Fixed side navigation | "Collapsible sidebar, 240px expanded, 64px collapsed" |
| `split-screen` | Two equal halves | "Split screen: image left, content right, reversed on mobile" |
| `F-pattern` | Top-left emphasis | "F-pattern layout: logo top-left, nav horizontal, content scans down-left" |
| `Z-pattern` | Diagonal visual flow | "Z-pattern landing: logo → nav → hero image → CTA" |

### Components
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `modal` | Overlay dialog | "Modal with backdrop blur, center aligned, close on outside click" |
| `drawer` | Slide-in panel | "Right drawer 400px wide, slides in from edge" |
| `sheet` | Mobile bottom panel | "Bottom sheet with drag handle, snap points at 25%, 50%, 100%" |
| `popover` | Floating content | "Popover on click, arrow pointing to trigger, auto-placement" |
| `tooltip` | Hover hint | "Tooltip on hover, 200ms delay, fade in" |
| `toast` | Temporary notification | "Toast bottom-right, auto-dismiss 5s, with undo action" |
| `snackbar` | Action notification | "Snackbar with action button, swipe to dismiss" |
| `accordion` | Collapsible sections | "Accordion: only one open at a time, animated height" |
| `tabs` | Tabbed content | "Tabs with underline indicator, animated slide" |
| `carousel` | Sliding content | "Carousel with dots, auto-play 5s, pause on hover" |
| `stepper` | Multi-step process | "Horizontal stepper with progress line between steps" |

### Navigation
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `breadcrumb` | Path trail | "Breadcrumb with chevron separators, current page not clickable" |
| `mega menu` | Large dropdown | "Mega menu with categories, featured items, and images" |
| `hamburger` | Three-line menu icon | "Hamburger that morphs to X, reveals full-screen nav" |
| `bottom nav` | Mobile footer nav | "Bottom nav 5 items, active state with label, safe area padding" |
| `command palette` | Keyboard launcher | "Cmd+K palette with fuzzy search, recent items, keyboard nav" |
| `spotlight search` | Global search overlay | "Spotlight: ⌘+Space, searches pages/actions/settings" |

### Feedback
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `loading spinner` | Rotation indicator | "Spinner 24px, primary color, replace button text while loading" |
| `progress bar` | Linear completion | "Progress bar with percentage label, animated fill" |
| `skeleton` | Content placeholder | "Skeleton matching card layout: avatar circle, 3 text lines" |
| `optimistic UI` | Assume success | "Optimistic update: show new comment immediately, revert on error" |
| `error states` | Failure display | "Error state: red border, icon, message below input" |

### Responsive
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `breakpoints` | Screen size thresholds | "Breakpoints: sm 640px, md 768px, lg 1024px, xl 1280px" |
| `fluid typography` | Scales with viewport | "Fluid type: clamp(1rem, 2vw + 0.5rem, 1.5rem)" |
| `container queries` | Size based on parent | "Container query: stack cards when container < 400px" |
| `mobile-first` | Start with mobile | "Mobile-first: single column default, grid at md breakpoint" |
| `adaptive` | Device-specific layouts | "Adaptive: different component set for mobile vs desktop" |

---

## 3. ARCHITECTURE PATTERNS

### Structural
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `MVC` | Model-View-Controller | "Structure as MVC: models in /models, views in /views, controllers handle logic" |
| `MVVM` | Model-View-ViewModel | "MVVM pattern: ViewModel binds data to View reactively" |
| `MVP` | Model-View-Presenter | "MVP: Presenter handles all logic, View is passive" |
| `Clean Architecture` | Dependency rule layers | "Clean Architecture: entities → use cases → adapters → frameworks" |
| `Hexagonal` | Ports and Adapters | "Hexagonal: core domain with ports, adapters for DB/API" |
| `Onion` | Concentric layers | "Onion: domain center, app services, infrastructure outer" |

### System Design
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `monolith` | Single deployable unit | "Start as monolith, modular structure for future splitting" |
| `microservices` | Independent services | "Microservice for auth, separate from main API" |
| `serverless` | Function-based | "Serverless functions for image processing, event-triggered" |
| `event-driven` | Events as primary mechanism | "Event-driven: publish OrderCreated, consumers handle separately" |
| `CQRS` | Command Query separation | "CQRS: separate read models from write operations" |
| `event sourcing` | Store events, not state | "Event sourcing: append-only event log, rebuild state from events" |

### Frontend Architecture
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `component-based` | UI as components | "Component-based: Button, Card, Layout components" |
| `atomic design` | Atoms → Organisms | "Atomic: atoms (button), molecules (search bar), organisms (header)" |
| `feature-sliced` | Vertical feature slices | "Feature-sliced: /features/auth, /features/dashboard each self-contained" |
| `module federation` | Runtime micro-frontends | "Module federation: shell app loads remote feature modules" |

### State Patterns
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `flux` | Unidirectional data flow | "Flux pattern: action → dispatcher → store → view" |
| `redux pattern` | Single store + reducers | "Redux: actions dispatch to reducer, update single store" |
| `signals` | Fine-grained reactivity | "Use signals for reactive state, auto-tracks dependencies" |
| `observables` | Stream-based state | "RxJS observables for event streams, pipe operators" |
| `pub/sub` | Publish-subscribe | "Pub/sub event bus for cross-component communication" |

### API Patterns
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `BFF` | Backend for Frontend | "BFF layer: aggregates microservices for mobile client needs" |
| `API Gateway` | Single entry point | "API Gateway: handles routing, auth, rate limiting" |
| `GraphQL federation` | Distributed schema | "Federated GraphQL: each service owns its subgraph" |

---

## 4. PROJECT MANAGEMENT / AGILE

### Scrum
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `sprint` | Fixed iteration period | "2-week sprint starting Monday" |
| `backlog` | Prioritized work queue | "Add to backlog, prioritize for next sprint" |
| `story points` | Relative effort estimate | "Estimate in story points: 1, 2, 3, 5, 8, 13" |
| `velocity` | Points per sprint | "Velocity: averaging 34 points per sprint" |
| `burndown` | Remaining work chart | "Generate burndown chart for sprint progress" |
| `standup` | Daily sync meeting | "Standup format: yesterday, today, blockers" |
| `retrospective` | Sprint review | "Retro: what went well, what to improve, action items" |

### Artifacts
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `epic` | Large feature set | "Epic: User Authentication (contains login, register, forgot password stories)" |
| `user story` | User-centric requirement | "Story: As a user, I want to reset password so I can recover access" |
| `task` | Technical work item | "Task: Implement password reset API endpoint" |
| `bug` | Defect report | "Bug: Login fails on Safari, expected: success" |
| `spike` | Research/exploration | "Spike: Evaluate auth providers, timebox 4 hours" |
| `tech debt` | Deferred quality work | "Tech debt: Refactor auth module, reduce coupling" |

### Concepts
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `MVP` | Minimum Viable Product | "MVP scope: core login, single dashboard view, basic CRUD" |
| `POC` | Proof of Concept | "POC: validate WebSocket performance under load" |
| `prototype` | Quick demonstration | "Prototype in Figma, clickable, 3 main flows" |
| `iteration` | Development cycle | "Iterate based on feedback, release weekly" |
| `increment` | Releasable chunk | "Each increment is deployable and adds value" |

### Kanban
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `WIP limits` | Work-in-progress caps | "WIP limit 3 for In Progress column" |
| `swimlanes` | Horizontal categories | "Swimlanes by team member or priority" |
| `lead time` | Request to delivery | "Track lead time: average 5 days" |
| `cycle time` | Start to completion | "Reduce cycle time by removing blockers" |
| `throughput` | Items completed/time | "Throughput: 8 items per week" |

### Planning
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `t-shirt sizing` | S/M/L/XL estimates | "T-shirt sizing for initial estimates, refine later" |
| `planning poker` | Team estimation game | "Planning poker: reveal estimates simultaneously" |
| `roadmap` | Long-term plan | "Quarterly roadmap with major milestones" |
| `milestone` | Key achievement point | "Milestone: Beta launch March 15" |

---

## 5. CODE QUALITY PRINCIPLES

### SOLID
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `Single responsibility` | One reason to change | "SRP: split UserService into UserAuth and UserProfile" |
| `Open-closed` | Open for extension, closed for modification | "OCP: use strategy pattern for payment methods" |
| `Liskov substitution` | Subtypes substitutable | "LSP: ensure all shapes implement area() correctly" |
| `Interface segregation` | Specific interfaces | "ISP: split IWorker into IWorkable and IFeedable" |
| `Dependency inversion` | Depend on abstractions | "DIP: inject IRepository, not PostgresRepository" |

### Other Principles
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `DRY` | Don't Repeat Yourself | "DRY: extract duplicate validation to shared util" |
| `KISS` | Keep It Simple | "KISS: simple if/else over complex pattern here" |
| `YAGNI` | You Aren't Gonna Need It | "YAGNI: don't add config options until needed" |
| `Separation of concerns` | Distinct responsibilities | "Separate data fetching from presentation logic" |
| `Composition over inheritance` | Compose behaviors | "Compose with hooks/mixins instead of class hierarchy" |

### Code Smells
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `god object` | Does too much | "Refactor god object: split into focused classes" |
| `spaghetti code` | Tangled logic | "Untangle spaghetti: extract functions, clear flow" |
| `shotgun surgery` | Changes spread everywhere | "Smell: shotgun surgery - consolidate related code" |
| `feature envy` | Uses other class's data | "Feature envy: move method to class it accesses most" |
| `primitive obsession` | Primitives over objects | "Replace primitives with Money value object" |

### Refactoring
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `extract method` | Pull out function | "Extract method: move lines 45-60 to calculateTotal()" |
| `inline` | Remove unnecessary abstraction | "Inline trivial wrapper function" |
| `rename` | Improve naming | "Rename: data → userProfile, fn → validateEmail" |
| `move` | Relocate code | "Move helper to utils, it's used across modules" |
| `decompose conditional` | Simplify if/else | "Decompose: extract isEligible() and isExpired()" |

### Testing
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `unit test` | Test single unit | "Unit test calculateDiscount with edge cases" |
| `integration test` | Test combined parts | "Integration test: API endpoint with database" |
| `e2e test` | Full user flow | "E2E test: complete checkout flow with Playwright" |
| `TDD` | Test-Driven Development | "TDD: write failing test first, then implement" |
| `BDD` | Behavior-Driven Development | "BDD: Given user logged in, When clicks logout, Then redirected" |
| `mocking` | Fake dependencies | "Mock API calls in tests, return fixed data" |
| `stubbing` | Replace with simple impl | "Stub database with in-memory store" |
| `fixtures` | Test data | "Fixtures: sample users, products for tests" |

---

## 6. FRONTEND PATTERNS

### React Patterns
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `compound components` | Related components share state | "Compound component: Tabs, TabList, Tab, TabPanel share context" |
| `render props` | Function as children | "Render props pattern for mouse position tracking" |
| `HOC` | Higher-Order Component | "HOC withAuth: wrap component, redirect if not logged in" |
| `custom hooks` | Reusable logic | "Custom hook useDebounce(value, delay)" |
| `context` | Prop drilling solution | "Context for theme, avoid passing through 5 levels" |
| `portals` | Render outside DOM hierarchy | "Portal modal to document.body" |

### State Management
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `local state` | Component-scoped | "Local state for form inputs, no need for global" |
| `lifted state` | Shared parent state | "Lift state to parent for sibling communication" |
| `global state` | App-wide state | "Global state for user session, cart" |
| `server state` | Remote data | "Server state with React Query, not Redux" |
| `URL state` | State in URL | "URL state: filters in query params, shareable" |

### Rendering
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `CSR` | Client-Side Rendering | "CSR for admin dashboard, no SEO needed" |
| `SSR` | Server-Side Rendering | "SSR for product pages, better SEO and LCP" |
| `SSG` | Static Site Generation | "SSG for blog posts, rebuild on content change" |
| `ISR` | Incremental Static Regen | "ISR: revalidate every 60s for semi-dynamic content" |
| `streaming` | Progressive rendering | "Stream SSR with Suspense boundaries" |
| `hydration` | Attach interactivity | "Hydration error: server/client mismatch" |
| `partial hydration` | Hydrate selectively | "Partial hydration: only interactive components" |
| `islands` | Interactive islands in static | "Astro islands: static page with React interactive parts" |

### Data Fetching
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `SWR` | Stale-While-Revalidate | "SWR: show stale data, fetch fresh in background" |
| `React Query` | Server state library | "React Query: useQuery for GET, useMutation for POST" |
| `suspense` | Async boundaries | "Suspense boundary with fallback loading UI" |
| `optimistic updates` | Assume success | "Optimistic: update UI immediately, rollback on error" |
| `infinite scroll` | Load more on scroll | "Infinite scroll with useInfiniteQuery, intersection observer" |

### Performance
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `virtualization` | Render visible only | "Virtualize list of 10,000 items with react-window" |
| `windowing` | Same as virtualization | "Windowing for table: only render visible rows" |
| `lazy loading` | Load on demand | "Lazy load below-fold components" |
| `code splitting` | Separate bundles | "Code split routes, each route own chunk" |
| `prefetching` | Load before needed | "Prefetch next page on link hover" |

---

## 7. BACKEND / API

### REST
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `CRUD` | Create Read Update Delete | "CRUD endpoints for /users resource" |
| `resources` | API entities | "Resource: /orders, /orders/:id, /orders/:id/items" |
| `endpoints` | URL paths | "Endpoints: GET /users, POST /users, PATCH /users/:id" |
| `status codes` | HTTP response codes | "201 Created for POST, 204 No Content for DELETE" |
| `idempotent` | Same result if repeated | "PUT is idempotent, POST is not" |
| `HATEOAS` | Hypermedia links | "Include _links for related resources in response" |

### GraphQL
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `query` | Read operation | "GraphQL query: fetch user with nested orders" |
| `mutation` | Write operation | "Mutation createUser with input type" |
| `subscription` | Real-time updates | "Subscription for new messages in chat" |
| `resolver` | Field data fetcher | "Resolver for user.orders, batch with DataLoader" |
| `schema` | Type definitions | "Schema: type User { id: ID!, name: String!, orders: [Order!] }" |
| `fragments` | Reusable field sets | "Fragment UserFields on User { id name email }" |

### Real-time
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `WebSocket` | Bi-directional connection | "WebSocket for live chat, handle reconnection" |
| `SSE` | Server-Sent Events | "SSE for notifications, one-way server to client" |
| `polling` | Periodic requests | "Poll every 30s for status updates" |
| `long-polling` | Hold request open | "Long-poll: server holds until new data or timeout" |

### Auth
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `JWT` | JSON Web Token | "JWT: access token 15min, refresh token 7days" |
| `OAuth` | Authorization framework | "OAuth 2.0 flow with Google provider" |
| `session` | Server-side state | "Session-based auth with Redis store" |
| `RBAC` | Role-Based Access Control | "RBAC: roles admin, editor, viewer with permissions" |
| `ABAC` | Attribute-Based Access | "ABAC: allow if user.department === resource.department" |
| `MFA` | Multi-Factor Auth | "MFA with TOTP, backup codes" |

### API Design
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `versioning` | API versions | "Version in URL: /api/v1/users or header Accept-Version" |
| `pagination` | Result pages | "Cursor pagination: ?cursor=abc&limit=20" |
| `filtering` | Query params | "Filter: ?status=active&createdAfter=2024-01-01" |
| `rate limiting` | Request caps | "Rate limit: 100 req/min per API key" |
| `throttling` | Slow down requests | "Throttle to 10 req/s, queue excess" |

---

## 8. DEVOPS / DEPLOYMENT

### CI/CD
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `pipeline` | Automated workflow | "Pipeline: lint → test → build → deploy" |
| `build` | Compile/bundle step | "Build step: npm run build, output to /dist" |
| `artifact` | Build output | "Store artifact in S3, reference in deploy" |
| `environment` | Deployment target | "Environments: dev, staging, production" |

### Deployment Strategies
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `blue-green` | Two identical envs | "Blue-green: deploy to green, switch traffic, keep blue as rollback" |
| `canary` | Gradual rollout | "Canary: 5% traffic to new version, monitor, increase" |
| `rolling` | Incremental update | "Rolling deploy: update 2 pods at a time" |
| `feature flags` | Toggle features | "Feature flag: enable new checkout for beta users" |
| `A/B testing` | Variant comparison | "A/B test: 50% see new CTA, measure conversion" |

### Containers
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `Docker image` | Container template | "Multi-stage Docker image, alpine base, under 100MB" |
| `container` | Running instance | "Run container with env vars from .env" |
| `volume` | Persistent storage | "Volume mount for database data persistence" |
| `compose` | Multi-container setup | "Docker Compose: app, db, redis services" |
| `Kubernetes pod` | Container group | "Pod with app container and sidecar for logging" |

### Infrastructure
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `IaC` | Infrastructure as Code | "IaC with Terraform, version controlled" |
| `serverless` | No server management | "Serverless function for image resize, triggered by S3" |
| `edge functions` | CDN-level compute | "Edge function for A/B testing, low latency" |
| `CDN` | Content Delivery Network | "CDN for static assets, cache 1 year with hash" |

### Monitoring
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `logging` | Event recording | "Structured JSON logs with request ID correlation" |
| `metrics` | Numerical measurements | "Metrics: request count, latency p50/p95/p99, error rate" |
| `tracing` | Request flow tracking | "Distributed tracing across services with OpenTelemetry" |
| `alerting` | Notification triggers | "Alert: error rate > 1% for 5 minutes" |
| `APM` | Application Performance | "APM dashboard: slowest endpoints, error traces" |
| `observability` | System understanding | "Full observability: logs, metrics, traces correlated" |

---

## 9. DATABASE

### Operations
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `CRUD` | Basic operations | "CRUD operations for users table" |
| `migration` | Schema changes | "Migration: add email_verified column, default false" |
| `seeding` | Initial data | "Seed database with test users and sample data" |
| `backup` | Data copy | "Daily automated backup to S3, 30 day retention" |

### Design
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `schema` | Structure definition | "Schema: users, posts, comments with relations" |
| `normalization` | Reduce redundancy | "Normalize to 3NF, extract address to separate table" |
| `denormalization` | Add redundancy for speed | "Denormalize: store author_name in posts for read performance" |
| `indexing` | Query optimization | "Index on (user_id, created_at) for user posts query" |
| `constraints` | Data rules | "Constraints: unique email, foreign key to users" |

### Scaling
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `replication` | Copy data across servers | "Primary-replica replication for read scaling" |
| `sharding` | Split data horizontally | "Shard by user_id hash, 4 shards" |
| `partitioning` | Split table by range | "Partition orders by month for faster queries" |
| `read replicas` | Read-only copies | "Route reads to replica, writes to primary" |
| `connection pooling` | Reuse connections | "PgBouncer pool: 20 connections, transaction mode" |

### Types
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `relational` | SQL, tables, joins | "PostgreSQL for relational data with complex queries" |
| `document` | JSON-like documents | "MongoDB for flexible schema, nested documents" |
| `key-value` | Simple lookup | "Redis for session store, cache" |
| `graph` | Nodes and edges | "Neo4j for social connections, recommendations" |
| `time-series` | Timestamped data | "InfluxDB for metrics, IoT sensor data" |
| `vector` | Embeddings for AI | "Pinecone for semantic search, RAG" |

### ORM Concepts
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `model` | Database entity class | "Prisma model User with fields and relations" |
| `repository` | Data access layer | "Repository pattern: UserRepository.findById()" |
| `query builder` | Programmatic queries | "Query builder: select().where().orderBy().limit()" |
| `eager loading` | Load relations upfront | "Eager load: include posts with user query" |
| `lazy loading` | Load relations on access | "Lazy load: fetch orders only when accessed" |

---

## 10. PERFORMANCE OPTIMIZATION

### Loading Strategies
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `lazy loading` | Load when needed | "Lazy load images below fold with loading='lazy'" |
| `eager loading` | Load immediately | "Eager load critical hero image" |
| `preloading` | Load before needed | "Preload font files in document head" |
| `prefetching` | Hint future needs | "Prefetch next page data on link hover" |
| `streaming` | Progressive loading | "Stream HTML, send head first for faster FCP" |

### Caching
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `memoization` | Cache function results | "Memoize expensive calculation with useMemo" |
| `browser cache` | Client storage | "Cache-Control: max-age=31536000 for hashed assets" |
| `CDN cache` | Edge caching | "CDN cache HTML 60s, assets 1 year" |
| `server cache` | Backend caching | "Redis cache for API responses, 5 min TTL" |
| `stale-while-revalidate` | Serve stale, refresh | "SWR: serve cached, revalidate in background" |

### Bundle Optimization
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `tree shaking` | Remove unused code | "Tree shake: only import needed lodash functions" |
| `code splitting` | Separate chunks | "Code split by route, vendor chunk separate" |
| `dynamic imports` | Load on demand | "Dynamic import() for modal component" |
| `minification` | Remove whitespace | "Minify JS/CSS in production build" |
| `compression` | Reduce size | "Gzip/Brotli compression for text assets" |

### Runtime
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `debouncing` | Delay until pause | "Debounce search input 300ms" |
| `throttling` | Limit frequency | "Throttle scroll handler to 16ms (60fps)" |
| `virtualization` | Render visible only | "Virtualize 10k row table with react-virtual" |
| `web workers` | Background threads | "Web worker for heavy JSON parsing" |
| `requestAnimationFrame` | Sync with display | "Use rAF for smooth animations" |

### Core Web Vitals
| Term | Definition | AI Prompt Example |
|------|------------|-------------------|
| `FCP` | First Contentful Paint | "Improve FCP: inline critical CSS, preload fonts" |
| `LCP` | Largest Contentful Paint | "LCP < 2.5s: optimize hero image, preload" |
| `TTI` | Time to Interactive | "Reduce TTI: code split, defer non-critical JS" |
| `TBT` | Total Blocking Time | "Lower TBT: break up long tasks, use workers" |
| `CLS` | Cumulative Layout Shift | "Fix CLS: set image dimensions, reserve space" |
| `TTFB` | Time to First Byte | "Improve TTFB: edge cache, optimize server" |

---

## Quick Reference: AI Prompt Templates

### Animation
```
"Add [effect] animation to [element] with [easing], [duration], triggered on [event]"
"GSAP timeline: [step1], then [step2], stagger [duration]"
"Framer Motion: initial [state], animate [state], exit [state], transition [config]"
```

### UI/UX
```
"Create [component] with [style] design, [behavior] on [event]"
"[Layout] pattern with [responsive] breakpoints"
"[Feedback] indicator while [async operation]"
```

### Architecture
```
"Structure as [pattern]: [layer1] → [layer2] → [layer3]"
"[Pattern] for [use case], separating [concern1] from [concern2]"
```

### Performance
```
"Optimize [metric]: [technique1], [technique2]"
"Cache [data] with [strategy], [TTL] expiry"
"Lazy load [resource] when [condition]"
```

### Testing
```
"[Test type] test for [function/component]: given [input], expect [output]"
"Mock [dependency] to return [data]"
"E2E: [user action] → [expected result]"
```

---

*Use these terms precisely in AI prompts for better, more specific code generation.*